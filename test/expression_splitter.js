const chai = require('chai');
const expect = chai.expect;

//demo test, to be filled with the expression parser test code
describe ('TBD', function (){
  it ('see that mocha is running properly', function () {
    expect(1).to.equal(1);
  });
});
